{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // Headless packages: no WebGPU or Vite ambient types, but node: imports
    // (zlib in the preview PNG writer) must resolve.
    "types": ["node"]
  },
  "include": ["src"]
}
